version: 0.2

phases:
  install:
    commands:
      - sudo apt-get update
      - sudo apt-get install nginx -y
        runas: root
  build:
    commands:
      - cp index.html /var/www/html/
  post_build:
    commands:
      - echo Configuring Nginx

artifacts:
  files:
    - '**/*'
